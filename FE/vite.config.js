// vite.config.js
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react"; // Import the React plugin

export default defineConfig({
  plugins: [react()], // Add the plugin to the Vite configuration
});
